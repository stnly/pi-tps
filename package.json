{
  "name": "pi-tps",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "tsc",
    "check": "tsc --noEmit"
  },
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@mariozechner/pi-coding-agent": "*"
  },
  "peerDependencies": {
    "@mariozechner/pi-coding-agent": "*"
  }
}
